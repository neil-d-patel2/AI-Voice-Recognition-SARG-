game = GameState(home_team="Yankees", away_team="Red Sox")

folder_path = "./mp3s"
file = record_audio()

shutil.move(file, "./mp3s")

for plays in os.listdir(folder_path):
     file_path = os.path.join(folder_path, plays)
     transcript = transcribe_audio(file_path)